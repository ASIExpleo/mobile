pipeline {
  agent any

stages {
  
stage('pre build') {
  steps {
         echo "checkout code from git----"
        }
               }

stage('build') {
  steps {
         echo "building----"
        }
               }

stage('post build') {
  steps {
         echo "email recipient-----"
         }
              }
      }
}
